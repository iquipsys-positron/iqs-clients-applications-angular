// import { TestBed, inject } from '@angular/core/testing';

// import { PipApplicationListComponent } from './application-list.component';

describe('a application-list component', () => {
    // let component: PipApplicationListComponent;

    // // register all needed dependencies
    // beforeEach(() => {
    //     TestBed.configureTestingModule({
    //         providers: [
    //             PipApplicationListComponent
    //         ]
    //     });
    // });

    // // instantiation through framework injection
    // beforeEach(inject([PipApplicationListComponent], (ApplicationListComponent) => {
    //     component = ApplicationListComponent;
    // }));

    // it('should have an instance', () => {
    //     expect(component).toBeDefined();
    // });
});
